export default class {
    constructor(params) {
        this.params = params;
    }

    setTitle(title) {
        document.title = title;
    }
        // Inserting HTML template ====================================================>
    async getHtml() {
    
        return ''
        ;
    }
        // Displaying the View Script ====================================================>
    async executeViewScript()
    {
        
    }
}

